
import CheckOut from '@/src/client/components/Chechout';
import type { NextPage } from 'next';

const CheckoutPage: NextPage = (props) => (
	<div>
		<CheckOut {...props} />
	</div>
);

export default CheckoutPage;
